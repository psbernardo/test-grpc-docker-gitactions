version: "3.8"

services:     
 web:
  build: .
  container_name: "grpc-go"
  ports:
   - "9010:9010"
   - "9011:9011"
  extra_hosts: 
   - "loghost:192.168.254.124"
  networks:
   - dockernet
 
networks:
 dockernet:
  external: true
   
  